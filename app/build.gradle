plugins { id 'com.android.application' }

android {
    namespace 'mx.taller3d.printmanager'
    compileSdk 34

    defaultConfig {
        applicationId "mx.taller3d.printmanager"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
